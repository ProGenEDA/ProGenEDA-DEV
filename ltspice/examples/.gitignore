# LTspice executable runs are immutable local/internal evidence.  They can
# contain proprietary simulator output, waveform binaries, and user archives;
# keep the generator, source fixtures, tests, and compact docs in Git instead.
progen_ltspice_executable_run_*/
e2e_oracle_fixtures_*/

!README.md
