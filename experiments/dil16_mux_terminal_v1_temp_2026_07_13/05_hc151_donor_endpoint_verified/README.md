# 74HC151 donor endpoint verification

Status: local Proteus loader gate passed; user visual acceptance pending.

This is a fresh 1x solo generated by the locked component-placement mega donor
and terminalized through `src/proteusgen/component_terminal_placer.py`. It is
not a donor copy and no component-specific terminal generator was added.

## Files

- `S01_74HC151_1X_NO_TERMINAL.pdsprj` — locked-mega component-placer control.
- `S01_74HC151_1X_CATALOGUE_TERMINAL_sa.pdsprj` — terminalized candidate.
- `local_proteus_gate/G01_74HC151_initial_before_close.png` — first visible
  cold-open capture.
- `local_proteus_gate/G04_74HC151_cold_reopen_before_close.png` — visible
  cold-reopen capture.

## Evidence

The authoritative donor is
`proteus_ic/donors/terminalized_catalogue_evidence/dil16_mux/74HC151/74HC151_user_terminalized_july04.pdsprj`.

The generated and donor `ROOT.DSN` object streams both contain fourteen
terminals and fourteen donor-shaped WIREs. Labels, angles, grid contacts,
WIRE marker offsets, full polylines, and the explicit final object-stream byte
match. Only active terminal/component link suffixes differ, because they are
rebased from the final generated WIRE addresses.

The local gate opened a copied candidate, waited for a visible schematic and
the required stability interval, captured the screen before close, then
cold-reopened the same untouched copy. No modal error appeared, so it was not
Ctrl+S-saved. Its SHA-256 remained equal to the generated candidate.

Focused tests:

```powershell
$env:PYTHONPATH = "src"
python -m pytest tests/test_component_placer.py -q -k "hc151 or hc76 or 4027"
```

Result: `5 passed`.
