# KiCad: 10 Complex Circuits

This is the simple handoff folder. Open any numbered folder, then open its
only `OPEN_THIS_PROJECT__*.kicad_pro` file. The project file is directly in
that folder; no run directories, reports, or generator metadata are required.

The original immutable generation records remain in
`../KiCad_10_Most_Complex_2026_07_19/` for auditing only.
