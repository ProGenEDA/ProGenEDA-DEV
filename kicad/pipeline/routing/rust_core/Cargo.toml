[package]
name = "progen_routing_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "progen_routing_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["auto-initialize"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"
