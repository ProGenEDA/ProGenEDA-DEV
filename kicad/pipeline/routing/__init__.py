"""Routing v2 package.

The package keeps the PDF-defined split explicit:

- ``routing.python`` owns JSON orchestration, config, reports, and fallback.
- ``routing.rust_core`` is the future EDA-neutral heavy geometry/routing core.
"""
