# KiCad Component Placer Run

Run label: `proteus_style_aliases_v1`

This folder was generated by `kicad.pipeline.kicad_component_placer`.
Generated project subfolders are immutable records; create a new run for any changed output.

## Purpose

This run locks the first Proteus-style alias expansion requested on
2026-07-02. The input contains 55 component kinds such as `VDC`, `RES`,
`POT-HG`, `1N4007`, `NE555`, `7SEGCOMA`, `4027`, `4511`, `7447`, `7490`, and
the requested `74HC*` logic devices.

## Result

- Input: `inputs/proteus_style_component_kind_smoke.json`
- Project: `projects/proteus_style_component_kind_smoke`
- Open: `projects/proteus_style_component_kind_smoke/OPEN_THIS_PROJECT__proteus_style_component_kind_smoke__PLACER.kicad_pro`
- Component count: 55
- Symbol instance count: 91
- Pipeline status: passed
- KiCad quality check: failed ERC gate, static checks passed

The symbol instance count is higher than the component count because several
real KiCad logic symbols are multi-unit parts. Generated schematic symbols are
real flattened KiCad library symbols; no `ProgenPlace` placeholder symbols are
expected in this run.

## Outcome

This run is retained as failed evidence. Static schematic validation passed, but
KiCad ERC reported six blocking `different_unit_net` violations for the first
`74HC266` substitute, `74xx_IEEE:74LS266`. That symbol repeats power pins across
gate units in a way that is noisy for no-net placement-only smoke schematics.

Validation summary:

- Static schematic check: passed
- KiCad ERC load: passed
- Blocking ERC violations: 6
- Blocking ERC type: `different_unit_net`
- Placement-only tolerated ERC violations: 687

Next run: `placer_run_2026_07_02_proteus_style_aliases_v2`, with `74HC266`
remapped to the real CMOS quad XNOR symbol `4xxx:4077`.

## Notes

Some requested Proteus names do not exist as exact KiCad 10.0.4 symbols in the
bundled library. Their closest mappings are recorded in
`kicad/pipeline/SUPPORTED_COMPONENTS.md`.
