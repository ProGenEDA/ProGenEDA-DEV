# ProGenEDA KiCad Executable Run

This immutable folder was produced by the single executable wrapper. It first validates/fixes input JSON into canonical main JSON, then runs the KiCad combination/terminal/wire project pipeline. The generated project run contains user-project zips and internal-only bundles.
