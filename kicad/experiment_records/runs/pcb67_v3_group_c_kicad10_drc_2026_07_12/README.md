# KiCad PCB CLI Oracle Run

This immutable evidence folder runs installed KiCad DRC against every PCB that the hosted source-backed validator accepted. KiCad CLI is an external release oracle and is not a hosted generator dependency. See `oracle_summary.json` and `reports/`.
